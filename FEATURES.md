📘 مستندات جامع فنی و اجرایی پروژه VPN (نسخه نهایی)
🏗 فصل اول: معماری کلان و زیرساخت (Architecture)
برای مدیریت ۳۰۰,۰۰۰ کاربر و تفکیک نقش‌ها، سیستم باید به صورت Microservices (یا ماژولار) طراحی شود.

۱. تکنولوژی‌های هسته (Tech Stack)
Backend (مغز متفکر): Laravel 11 (PHP 8.2+). تمام لاجیک بیزنس، محاسبات مالی و ارتباط با سرورها اینجا انجام می‌شود.

Telegram Bot (رابط کاربری ربات): Python (استفاده از کتابخانه Aiogram یا Pyrogram به صورت Async). ربات فقط یک "پوسته" است که به APIهای لاراول درخواست می‌فرستد.

Frontend (داشبورد وب): React.js + Shadcn/ui + Tailwind CSS. (SPA - Single Page Application).

Database (حافظه مشترک): PostgreSQL (نسخه ۱۵+). به دلیل قدرت بالا در مدیریت Concurrency و تراکنش‌های مالی.

Caching & Queues (شتاب‌دهنده): Redis. برای مدیریت Sessionهای ربات، کش کردن دیتابیس و صف‌بندی پیام‌های انبوه.

VPN Core: پنل Marzban (نصب شده روی چندین سرور مختلف).

۲. معماری شبکه (Network Topology)
Main Server: میزبان لاراول، دیتابیس، ردیس و فرانت‌اند.

Edge Servers (Nodes): سرورهای خارج (آلمان، ترکیه و...) که فقط مرزبان روی آن‌ها نصب است.

Load Balancer Logic: لاراول تصمیم می‌گیرد کاربر جدید روی کدام سرور ساخته شود.

🗄 فصل دوم: پایگاه داده (Database Schema)
این بخش حیاتی‌ترین قسمت برای یکپارچگی سیستم است.

۱. جداول کاربران و نقش‌ها
users:

id, telegram_id (nullable for resellers), username, password (for web login), role (admin, reseller, affiliate, user), wallet_balance (موجودی ریالی), credit_limit (سقف بدهی نماینده), current_debt (بدهی جاری نماینده), parent_id (آی‌دی معرف یا نماینده), created_at.

reseller_profiles: (اطلاعات تکمیلی نمایندگان وب)

user_id, brand_name, contact_number, is_active.

۲. جداول سرویس و سرور
servers:

id, name (n1-Germany), flag_emoji (🇩🇪), ip_address, api_domain, admin_user, admin_pass, capacity (ظرفیت کل), active_users_count (پر شده), type (single, multi_relay), location_tag (DE, TR, FR), is_active.

plans:

id, name (یک ماهه ویژه), price_base, duration_days, traffic_bytes, max_concurrent_users (ضریب قیمت دارد), description.

۳. جداول اشتراک و فروش
subscriptions:

id, user_id, server_id (nullable if multi), uuid, marzban_username, status (active, expired, banned), total_traffic, used_traffic, expire_date.

subscription_links: (برای حالت مولتی سرور)

subscription_id, server_id, vless_link.

۴. جداول مالی
transactions:

id, user_id, amount, type (deposit, purchase, reseller_payment, commission), gateway (zibal, card_to_card, system), status, proof_image (برای کارت به کارت), ref_id.

invoices: (صورت‌حساب ماهانه نمایندگان)

id, reseller_id, start_date, end_date, total_amount, status (paid, unpaid), file_path (PDF).

🖥 فصل سوم: امکانات و منوهای کامل (به تفکیک دسترسی)
در این بخش، تمام دکمه‌ها و قابلیت‌های هر نقش را لیست می‌کنیم.

۱. 👑 سوپر ادمین (Super Admin) - دسترسی: پنل وب + ربات
الف) داشبورد وب (React Admin Panel):
Dashboard Home:

نمودار فروش زنده (روزانه/ماهانه).

وضعیت سرورها (Health Check): نمایش CPU/RAM و ظرفیت هر نود مرزبان.

لیست آخرین تراکنش‌ها و تیکت‌ها.

Server Management:

افزودن سرور جدید (IP, Port, Cert).

تنظیم ظرفیت (Capacity Limiter).

خاموش/روشن کردن سرور (Maintenance Mode).

User & Reseller Management:

لیست کامل کاربران با فیلتر پیشرفته.

ساخت نماینده جدید (تنظیم Credit Limit).

مشاهده زیرمجموعه‌های هر نماینده.

Financials:

تایید/رد واریزی‌های کارت‌به‌کارت.

مشاهده درخواست‌های تسویه حساب بازاریاب‌ها.

صدور فاکتور دستی برای نمایندگان.

Plans & Pricing:

تعریف قیمت پایه.

تعریف ضرایب (مثلاً: دو کاربره = ۱.۸ × قیمت پایه).

ب) دسترسی ربات (Admin Bot Menu):
دستور /admin پنل را باز می‌کند.

📊 آمار سریع: تعداد یوزر، فروش امروز.

💳 تایید تراکنش: دریافت عکس رسیدهای کارت‌به‌کارت و دکمه [✅ تایید] / [❌ رد].

📢 ارسال همگانی: ارسال پیام به تمام ۳۰۰ هزار نفر (با صف‌بندی Async).

۲. 💼 نماینده فروش (Web Reseller) - دسترسی: پنل وب اختصاصی
این افراد در ربات فعالیت ندارند (یا فعالیت محدود دارند). اصل کار آنها در وب است.

داشبورد وب نماینده (Reseller Panel):
Dashboard:

اعتبار باقی‌مانده (Credit Limit - Debt).

تعداد کاربران فعال من.

لینک اختصاصی کانال/پشتیبانی (White Label - اختیاری).

User Management:

➕ ایجاد کاربر: (انتخاب پلن -> انتخاب لوکیشن -> ایجاد -> کسر از اعتبار -> دریافت لینک).

مدیریت کاربران: تمدید، تغییر اکانت، دریافت QR کد، حذف یوزر.

نکته: نماینده فقط یوزرهای خودش را می‌بیند.

Accounting (حسابداری):

مشاهده ریز تراکنش‌ها (هر یوزری که می‌سازد یک رکورد منفی ثبت می‌شود).

مشاهده فاکتورهای ماهانه.

دکمه "پرداخت بدهی" (اتصال به درگاه برای صاف کردن حساب).

Support:

ارسال تیکت به سوپر ادمین.

۳. 🗣 بازاریاب عادی (Affiliate) - دسترسی: فقط ربات
این‌ها کاربرانی هستند که فقط لینک پخش می‌کنند.

منوی ربات بازاریاب:
(دسترسی‌های عادی مشتری را دارد) + دکمه 💰 کسب درآمد.

🔗 دریافت لینک دعوت: لینک اختصاصی (t.me/bot?start=12345).

📊 آمار من: تعداد دعوت شده‌ها، تعداد خریدها، درآمد کل.

💳 تسویه حساب:

اگر موجودی > حد نصاب (مثلاً ۵۰۰ هزار تومان).

دریافت شماره کارت.

ثبت درخواست برای ادمین.

۴. 👤 مشتری نهایی (End User) - دسترسی: فقط ربات
منوی اصلی (Reply Keyboard):
🛍 خرید سرویس (Purchase): شروع فلو خرید.

🔧 سرویس‌های من (My Services):

لیست سرویس‌ها به صورت دکمه شیشه‌ای.

با کلیک روی سرویس:

📊 مشاهده حجم باقی‌مانده/زمان.

🔗 دریافت لینک اتصال (لینک Subscription).

📲 دریافت کد QR.

🔄 تمدید سرویس (Renew).

⚙️ تغییر لوکیشن (اگر مجاز باشد).

👤 پروفایل و کیف پول:

موجودی کیف پول.

افزایش موجودی (شارژ).

آی‌دی عددی کاربر.

🍏 آموزش اتصال: (iOS, Android, Windows, Mac).

📞 پشتیبانی: ارسال پیام به ادمین/ساپورت.

🧪 تست رایگان: (یک بار برای هر شماره موبایل).

🔄 فصل چهارم: فلوهای عملیاتی دقیق (Business Logic Flows)
فلو ۱: لاجیک هوشمند انتخاب سرور (Server Selection Logic)
وقتی کاربر (یا نماینده) می‌خواهد سرویس بخرد:

فیلتر اولیه: سیستم سرورهای فعال (is_active=1) را لیست می‌کند.

بررسی ظرفیت:

برای هر سرور چک می‌کند: if (active_users_count < capacity).

سرورهای پر شده از لیست حذف می‌شوند.

نمایش به کاربر: لیست لوکیشن‌های باقی‌مانده نمایش داده می‌شود (مثلاً: 🇩🇪, 🇳🇱).

Load Balancing (توزیع بار):

اگر کاربر "آلمان" را انتخاب کرد و شما ۵ سرور آلمان دارید.

سیستم به سروری وصل می‌شود که کمترین active_users_count را دارد (Least Connection Algorithm).

فلو ۲: خرید "مولتی سرور" (Multi-Server Provisioning)
اگر کاربر گزینه "🌍 تمام لوکیشن‌ها (VIP)" را انتخاب کرد:

سیستم لیست همه سرورهای فعال (آلمان، هلند، ترکیه و...) را می‌گیرد.

حلقه ایجاد (Creation Loop):

سیستم یک UUID ثابت تولید می‌کند (برای راحتی کاربر).

به تمام سرورها درخواست POST /user می‌فرستد.

لینک‌های VLESS برگشتی از همه سرورها جمع‌آوری می‌شود.

تحویل:

یک "لینک اشتراک" (Subscription Link) تولید می‌شود که به دامنه اصلی شما اشاره دارد (sub.mydomain.com/api/sub/{uuid}).

وقتی کاربر این لینک را در کلاینت (v2rayNG) ایمپورت می‌کند، کلاینت به لاراول وصل می‌شود و لاراول لیست تمام کانفیگ‌ها را از دیتابیس (subscription_links) خوانده و برمی‌گرداند.

این باعث می‌شود اگر فردا سرور جدید اضافه کردید، کاربر با یک "Update Subscription" ساده سرور جدید را دریافت کند.

فلو ۳: چرخه حیات سرویس (Service Lifecycle)
ایجاد: کاربر فعال شد.

مانیتورینگ: جاب (Cron Job) هر ۱۰ دقیقه اجرا می‌شود و حجم مصرفی را از مرزبان‌ها می‌گیرد و در دیتابیس لوکال آپدیت می‌کند.

هشدار:

حجم باقیمانده < ۲۰٪ -> ارسال پیام هشدار در تلگرام.

زمان باقیمانده < ۳ روز -> ارسال پیام "زمان تمدید".

انقضا:

اگر حجم یا زمان تمام شد -> درخواست Disable User به مرزبان ارسال می‌شود.

وضعیت در دیتابیس Expired می‌شود.

حذف:

اگر ۳۰ روز از انقضا گذشت و تمدید نشد -> یوزر کامل از مرزبان و دیتابیس پاک می‌شود (برای خالی شدن ظرفیت).

فلو ۴: سیستم مالی نماینده (Reseller Credit System)
نماینده یک یوزر ۵۰ هزار تومانی می‌سازد.

سیستم چک می‌کند: (Bedehi_Feli + 50,000) <= Saghe_Etebar.

اگر OK بود: سرویس ساخته می‌شود و ۵۰,۰۰۰ به Bedehi_Feli اضافه می‌شود.

سر ماه: سیستم فاکتور می‌سازد.

نماینده پرداخت می‌کند -> Bedehi_Feli صفر می‌شود.

🛡 فصل پنجم: امنیت و پرفرمنس (برای ۳۰۰ هزار کاربر)
Rate Limiting (تلگرام): استفاده از Redis برای محدود کردن تعداد درخواست هر کاربر (مثلاً ۵ درخواست در دقیقه) تا ربات زیر بار اسپم هنگ نکند.

Job Queues (صف‌ها): هیچ عملیات سنگینی (مثل درخواست به مرزبان یا ارسال پیام همگانی) نباید در Thread اصلی انجام شود. همه باید به Laravel Horizon (صف) منتقل شوند.

Database Indexing: ستون‌های telegram_id, uuid, parent_id حتما باید ایندکس شوند تا سرعت جستجو زیر ۱۰ میلی‌ثانیه باشد.

Webhook Security: اطمینان حاصل کنید که وب‌هوک تلگرام فقط از IPهای تلگرام درخواست می‌گیرد (یا با Secret Token چک شود).

Backup: بکاپ روزانه از دیتابیس SQL و تنظیمات مرزبان (/var/lib/marzban) به یک فضای ابری (S3/MinIO).

این ساختار، کامل‌ترین فرم برای پیاده‌سازی سیستمی است که هم پایدار باشد و هم قابلیت درآمدزایی چندکاناله (کاربر، بازاریاب، نماینده) را داشته باشد.